import React from "react";
import useAuth from "./useAuth";
import { auth } from "firebase-admin";

const useRefreshToken = () => {

    const { useAuth } = useAuth();

    const refresh = auth.rev


    return 
    <div>

    </div>
}

export default useRefreshToken